# V2 archive (frozen)

Snapshot of the site as of the V2 freeze (June 2026). **Do not edit** these files for new work.

| File | Purpose |
|------|---------|
| `index.html` | Full page (uses `../../img/` for assets) |
| `styles.css` | V2 styles scoped to `.page-v2` |
| `work-carousel.js` | Case study carousels |
| `COPY-v2.md` | Copy at time of freeze |

## Preview

- **GitHub Pages:** `https://work.jeremyafinch.com/archive/v2/` (noindex)
- **Local:** open `archive/v2/index.html` in a browser, or run `./preview.sh` from repo root and visit `/archive/v2/`

## Revert

To restore this version at the site root, copy these files back to the repo root (or check out git tag `v2.0`).
